#include "MineField.h"

MineField::MineField(size_t width, size_t height, int mines) 
    : width(width), height(height), mines(mines)
{
    // width, height, and mines are  already
    // initialized above in the initializer list
    
    // Construct a MineField and initialize it with Mines in random positions,
    // NumberedTiles adjacent to those Mines, and EmptyTiles where there are no
    // adjacent mines 
    
    // see 
    //      https://en.cppreference.com/w/cpp/numeric/random
    // for random number libraries and usage
    
    // ---
    
    
    
    
    
    
    // ---
}

